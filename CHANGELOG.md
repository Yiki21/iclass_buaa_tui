# ChangeLog

## 0.8.2

修复发布构建在 GitHub runner 上使用本机专用 linker 参数失败的问题：移除 `.cargo/config.toml` 中的 `--ld-path=wild` 和 `-Zshare-generics`，统一使用 runner 默认 linker；质量门禁与发布构建固定使用 `nightly-2026-09-21`。

## 0.8.0

本版重点修复研讨室 TUI、长列表交互和异步状态归属，并加强自动化请求的安全性。

### 修复

- **研讨室 TUI 改为独立直连 SSO-only 会话**：不再复用 WebVPN/iClass 主会话，避免 `未获取到研讨室 SSO Token`。房间列表、时段、预约记录、预约和取消都使用同一条独立会话链。
- 研讨室 TUI 新增联系电话、主题、日期、人数表单，并支持房间切换、时段选择、预约记录和写操作确认。
- 成绩、考试、空教室和作业列表支持上下键、`j/k`、鼠标滚轮、`PageUp/PageDown`、`Home/End`，选中项会自动滚入可视区域。
- 退出登录会清空各业务页签状态；旧账号的异步回包会被丢弃，读任务会取消。
- 签到结果按请求中的课程 ID 更新，评教问卷按课程任务绑定；评教只在收到成功结果后标记为已完成，批量失败项会保留待评状态。
- 考试日期统一解析日期时间格式，避免真实上游返回 `YYYY-MM-DD HH:MM:SS` 时考试提醒消失。

### 安全与可靠性

- 博雅读取请求设置上限并限制并发；选课、退选和签到不再在响应丢失、超时或 5xx 后盲目重放。
- 研讨室预约提交不再自动重复发送，结果未知时提示查询预约记录。
- HTTP 客户端增加连接、读取和总请求期限；诊断信息脱敏 URL 和敏感响应。
- 增加 push/PR CI 门禁，发版构建依赖格式、编译、测试和 Clippy 检查。

### 验证

- `cargo fmt --check` 通过。
- `cargo check --locked` 通过。
- `cargo test --locked`：153 passed。
- 新增本地模拟统一认证 → CGYY cookie → 研讨室 token 的测试，不执行真实预约、签到、打卡或评教提交。


## 0.7.0

本版新增研讨室、图书馆座位、阳光打卡、评教四个模块（CLI + TUI），并把 CLI 改造成适合自动化调用的形态。

### 破坏性变更

- **`plan` 与 `sign` 现在需要 `--yes`**。两者会写入真实签到状态，此前无需确认即可执行。不加 `--yes` 时只打印预览（`--json` 输出 `submitted: false`），不会签任何东西。
- **已有定时任务需要重装**：`install-autologin` 生成的 systemd / launchd / 计划任务命令现在带 `--yes`。旧任务会变成「只预览」，签到不会发生。请重新运行 `iclass_buaa_tui install-autologin --yes`（先 `uninstall-autologin --yes` 可选）。
- **`install-autologin` / `uninstall-autologin` 也需要 `--yes`**。
- 直连模式配置里带有统一认证密码时，配置文件权限现在也必须是 `600`。此前该校验只在 `use_vpn = true` 时生效，而直连模式同样使用统一认证密码。

### 安全

- 升级依赖修掉 Dependabot 报的 high 漏洞：`quinn-proto` 0.11.14 → 0.11.18（远程内存耗尽）、`rustls` 0.23.40 → 0.23.45（TLS 1.3 握手跨加密层接受消息）、`crossbeam-epoch` 0.9.18 → 0.9.21。`cargo audit` 现在无漏洞告警。
- `rsa` 的 Marvin Attack 计时侧信道在 `.cargo/audit.toml` 里显式忽略并写明原因：本仓库只用它做公钥加密（BYKC 密码），不涉及私钥解密运算，没有可泄漏的私钥；上游无修复版本。

### 修复

- **研讨室（CGYY）已真机修复**：此前在 VPN 模式下始终报「未获取到研讨室 SSO Token」。根因是登录流程「先统一认证、后 iClass」——研讨室只依赖统一认证会话，但 iClass 是校内服务，离开校园网/走代理时它超时，于是整条登录被判失败，而研讨室真正需要的会话其实早已建立。现在研讨室只执行统一认证阶段（新增 `login_sso_only`，并把该阶段从 `start_login` 中拆出），且**始终使用直连**（该服务与统一认证均可公网直连；WebVPN 会把 SSO cookie 落网关上，导致握手匿名、拿不到 token）。实测两种配置下均返回真实房间列表与时段。

- 补充排查文档：校内服务不可用的根因是 TUN 把校内域名解析成 fake-IP（`198.18.0.0/16`），而非路由拦截——把 `10.0.0.0/8` 排除出 TUN 只解决一半，域名先解析成 fake-IP 就永远轮不到路由规则。实测关闭 TUN 后 `cgyy`、`sso` 立刻从 `000` 恢复为 `302`/`200`。同时记录：改 `mihomo.yaml` / `work/config.yaml` 都不持久，GUI 会在运行时重新生成配置。

- 考试安排此前直接请求 BYXT 接口，得到 `HTTP 401`：BYXT 只有在入口页被访问过（经 SSO 链）之后才会下发自己的会话 cookie，成绩查询路径原本就有这一步而考试路径漏了。现在考试也先访问门户再查询，401 已消失，但随即暴露为 `HTTP 404`（接口路径/参数问题），**尚未解决**。
- 同步上游 UBAA 至 v1.8.1（20 个提交）。其中 `VpnCipher` / `LocalWebVpnSupport` 修复了 WebVPN 地址重写丢失路径尾斜杠（`/web/` 变 `/web`）导致登录循环的问题。已核对本仓库的 `to_webvpn_url`：`url` crate 的 `path()` 本身返回已编码路径且保留尾斜杠，**不存在该缺陷**，但补充了两个回归测试分别固定「尾斜杠保留」与「片段 token 不被复制进请求查询」。
- **考试安排已修复**：`HTTP 404` 的根因是上一版新增的门户激活步骤请求了 `/jwapp/sys/homeapp/index.html`——该页面直接请求返回 404，只能作为 `Referer` 使用。改用门户根路径激活后，401 与 404 一并消失。实测：`2025-2026-1` 返回「编译技术 2025-12-18 10:00-12:00 三号楼(211) 座位36」，`2024-2025-1` 返回「算法分析与设计」。

- **本科课表 / 签到 / 博雅在 TUN 代理下全部不可用**：根因是代理接管了校内网段。实测同一台机器上，`10.20.11.166:8347` 走代理时 TLS 握手被断开、绑定物理网卡立即返回 404；已确认为 Clash/Mihomo 的 TUN 模式把 `10.0.0.0/8` 也代理了（`ip route get` 显示 `dev Mihomo`）。修复方式是在代理配置里把校内网段排除出隧道，见 `docs/clash-campus-direct.md`。
- **研讨室登录被拒绝**：`sso/manageLogin` 与 `api/login` 此前被错误地走了 WebVPN 改写地址，导致拿不到 `sso_buaa_zhjs_token`；且登录请求本身漏了签名，服务端返回 `{"code":400,"Bad Request"}`。现在握手始终直连且走完整签名路径。
- **研讨室场地列表解析错误**：服务返回的是「场地 → 房间」两级结构，而后续所有接口要的是**房间** id。此前直接用场地 id，`venue-slots` 报 `code=250 无效的场地ID`（如 107/111 是场地，1/4/5/12/21 才是房间）。现在解析时展平为每个房间一行。
- **研讨室时段解析错误**：真实响应是 `data.reservationDateSpaceInfo[日期][房间][时段id]`，时段是对象的数字键而非数组，可约性看 `reservationStatus`（1 可约、4 不可约），预约上下文 token 在 `data.token`。此前按不存在的字段名解析，结果总是「该日期没有可预约的房间」。
- **图书馆座位无法连接**：`booking.lib.buaa.edu.cn` 只发送叶证书、不带中间证书且没有 AIA 地址，任何标准客户端都无法构建信任链（`curl` 同样失败）。现在为图书馆单独使用一个客户端并信任内置的缺失中间证书，验证仍然完整执行，只是额外信任这一张证书；作用范围仅限该主机。
- **图书馆 CAS 参数名错误**：该服务把票据放在 `cas` 参数里而不是标准的 `ticket`，此前取不到值；补上模糊匹配后又误把 SSO 服务地址里的 `login%2Fcas` 当成票据，导致登录返回 `code=1 操作失败`。现在只匹配查询串形式的 `?cas=` / `&cas=`，并加了对应回归测试。
- **图书馆请求头缺失**：缺少 `Accept` 与 `X-Requested-With`，服务会拒绝非 XHR 请求。
- 图书馆客户端补齐浏览器请求头与共享 cookie jar，并对票据做百分号解码；`list-today` 建立的会话现在能被图书馆请求复用。
- **研讨室在 VPN 模式下取不到 SSO Token**：握手必须与建立会话的模式一致。直连时会话 cookie 在 `cgyy.buaa.edu.cn` 上，VPN 模式时统一认证会话建立在 `d.buaa.edu.cn` 上、证明会话的 cookie 也在该主机，因此握手与读取 cookie 都要跟随当前模式；此前固定直连，在 VPN 模式下等于匿名访问。

- 修正上一版对「TLS 握手被断开」的解释：真正原因是**本机代理 / VPN 把校内网段也接管了**，不是校内地址在校外不可达、也不是改用 WebVPN 能解决。实测同一台机器上走代理时 `iclass.buaa.edu.cn:8346` 连接被断开，绕过代理后立即返回 302。错误信息与 `doctor` 建议现在直接指向代理绕过设置（`ip route get 10.20.11.166` 可确认是否走到了 Mihomo / tun0）。

- **Tab 键切不到全部页签**：`switch_workspace_tab` 里还留着一份写死的三个页签数组，所以顶栏能看到七个页签，但用 `tab` / `shift+tab` 只能在课表 / 签到 / 博雅之间循环。现在改为从 `WorkspaceTab::ALL` 生成，并加了遍历一整轮的回归测试。
- **研讨室在 VPN 模式下报「未获取到研讨室 SSO Token」**：`sso/manageLogin` 与 `api/login` 被错误地走了 WebVPN（`d.buaa.edu.cn/...`），而 `sso_buaa_zhjs_token` 是投给 `cgyy.buaa.edu.cn` 的，于是既拿不到 cookie 也找不到它。现在与上游实现一致：这两个握手请求始终直连，cookie 也从直连地址读取。设置 `ICLASS_CGYY_DEBUG=1` 可打印实际存在的 cookie 名称。
- **登录失败信息不再误导**：iClass 使用校内地址（`iclass.buaa.edu.cn` 解析到 `10.x`），校外网络会在 TLS 握手阶段被直接断开。此前这种情况只报「网络异常」并当作可重试；现在单独识别为「无法建立加密连接」且不再重试，并明确提示需要校园网或改用 WebVPN。

### 新增

- 失败时输出稳定错误码：带 `--json` 调用失败会在 stderr 输出 `{ error, command, retryable, code }`，`code` 取值如 `not_authenticated` / `invalid_argument` / `rate_limited` / `upstream_timeout` / `account_locked` / `resource_unavailable` / `network_error` / `config_invalid` / `unknown`，并给出 `retryable`。调用方不必解析人类可读的错误文本就能决定重试还是重新登录。
- 无法识别的失败如实报 `unknown` 且 `retryable: false`，不猜测——猜错会让重试循环浪费额度。

- 新增 `schema` 命令：输出机器可读的全部命令清单（参数、是否写操作、确认标志、是否支持 `--json`）。AI 或脚本不必再解析 `--help` 文本。参数直接从 clap 反射得到，因此不会与真实解析器不一致。
- `schema` 把命令分为 `read` / `write` / `write_irreversible` 三类：打卡与评教无法在本工具内撤销，明确标出；预约类可撤销。确认标志也是从解析器里读出来的，没有确认标志的命令不会谎报有。
- 修复 `| head` 导致的 panic：Rust 默认忽略 SIGPIPE，管道读端关闭后下一次写会 panic 并打印 `Broken pipe`。现在恢复 SIGPIPE 默认行为，进程安静退出，退出码正常。这个问题影响所有命令。

- 所有命令都支持 `--json`。此前只有查询类命令有，六个写命令（`venue-reserve` / `seat-book` / `clockin-submit` / `eval-submit` / `sign` / `plan`）只有人读文本，自动化无法可靠解析结果。
- 写命令的 `--json` 同时覆盖**预览与提交**两种结果：不加 `--yes` 时输出 `"submitted": false` 与 `would_reserve` / `would_submit` 字段，加 `--yes` 后输出 `"submitted": true` 与结果。这样调用方可以用同一套解析逻辑先规划再执行。
- `sign` 默认输出改为一行摘要（原来无论是否要 JSON 都打印完整 JSON），需要结构化结果时加 `--json`。

- TUI 新增四个工作区页签：**研讨室 / 图书馆 / 打卡 / 评教**，与课表、签到、博雅并列，共七个页签（`tab` / `shift+tab` 切换，也可点顶栏切换）。
- 研讨室页签：房间列表 → 某天的时段（`enter` 选中/取消，可多选连续时段）→ `s` 预约；`O` 查看自己的预约、`x` 取消。
- 图书馆页签：图书馆与空闲数 → 阅览区与座位（可约/已占用）→ `enter` 预约；`B` 查看预约、`x` 取消。
- 打卡页签：达标进度（本学期次数/要求、周/月/今日）、可打卡项目、`h/l` 切类别、`t` 查看记录。
- 评教页签：待评课程（已评/未评）、`enter` 查看问卷**以及将被选中的选项**、`s` 评这门、`S` 评全部。
- 所有写操作在 TUI 里都要经过确认弹窗：弹窗说明将要做什么、后果是什么，只有 `y` / `enter` 才继续，`n` / `esc` / `q` 一律取消。评教的确认弹窗会明确写出「将以你的名义作答」。
- 顶栏让位给七个页签：待办提示条移到状态行右侧（仍可点击跳转），版本号移到页脚。

- 新增评教 CLI：`eval` 列出待评课程（含已评/未评），`eval --show <rwid>` 查看某门课的问卷**以及将会提交的答案**；`eval-submit --task <rwid>` 或 `--all` 提交。
- 评教提交是「以你的名义、在你没有逐题阅读的情况下」作答，且不可撤销，因此：不加 `--yes` 只打印每门课的题目与将要选中的选项并停止；`eval-submit` 必须先 `--task` 或 `--all` 指定范围，不会默认全量提交。
- 提交前会先加载全部问卷再统一预览，避免「已经提交了一部分才看到内容」。

- 新增阳光打卡 CLI：`clockin` 查看达标进度（本学期次数/要求、周/月/今日次数、可打卡项目，可加 `--records` 看历史）、`clockin-submit` 提交打卡。
- 打卡提交要求 `--yes` 且必须提供 `--photo`（服务端强制要求照片）；不加 `--yes` 只打印预览（类别、项目、地点、时间、照片路径），不会写入记录。时间默认「结束=现在、开始=40 分钟前」，可用 `--start` / `--end` 指定，时间按北京时间解析并按东八区格式化。
- 修复阳光打卡 OAuth code 解析：code 位于 SPA 路由的 `#` 之后（`/#/home?code=...`），`Url::query_pairs` 读不到 `#` 之后的部分，会一直取不到 code 导致登录失败；现在同时检查 query 与 fragment。

- 新增图书馆座位（图书馆预约系统）CLI：`seats` 查图书馆与阅览区空闲、`seat-book` 预约座位、`seat-orders` 查看/取消预约。
- 座位预约同样要求 `--yes`：不加只打印预览（阅览区、座位号、日期、时段、实时可约状态），误输入不会占掉真实座位。取消预约也要 `--yes`。
- 座位预约请求按上游要求用 AES-128-CBC 加密（`aesjson` 字段），密钥由预约日期派生（8 位数字 + 其反序），固定 IV；日期格式不对会直接报错而不是用错误密钥发请求。

- 界面不再出现英文缩写：顶栏与帮助里的 `iClass` / `BYKC` 改为「签到」/「博雅」，应用名改为「智慧北航 TUI」。原来的英文缩写对使用者没有意义，中文课程系统里也不这么叫。
- 新增研讨室（场馆预约 CGYY）CLI：`venues` 列出可预约房间与用途、`venue-slots` 查看某天各时段是否可约、`venue-reserve` 预约、`venue-orders` 查看/取消预约并显示门锁码。
- 预约写操作需要显式 `--yes`：不加时只打印「将要预约什么」的预览，因此误输入命令不会占掉一间真实房间。取消预约同样要求 `--yes`。
- 研讨室请求签名（app-key + timestamp + MD5）与滑块验证码自动求解已实现；验证码在提交时会重新获取并最多重试 3 次。设置 `ICLASS_CGYY_CAPTCHA_DEBUG=1` 可打印识别出的偏移与明文，便于排查验证码失败。

- 待办指示条新增最近截止倒计时：`2 待交 · 最近 18h`。按紧迫度着色（24 小时内红加粗、72 小时内黄、更远灰），单位取最粗可读的一档（`45m` / `18h` / `3d`）。
- 今日课程页在最近 7 天有考试时，于列表上方显示一行考试横幅：倒计时（今天 / 明天 / N 天后）、课程、日期时间、地点、座位。
- 登录后自动预取考试安排，这样今日页的考试提醒不需要先去点「考试」视图；尚未导入课表导致学期未知时静默跳过，之后按 `r` 仍可手动画取。
- 签到失败新增桌面通知（`notify_on_failure`，默认开启）：账号被锁、会话过期这类「跑完了但什么都没签上」的情况会主动提醒，不必再靠翻调度器日志发现。
- 新增 `iclass_buaa_tui notify` 测试通知能否发出；缺 `notify-send` 或无图形会话时会明确报出原因和建议。

- 顶栏新增待办指示条：一眼看到「几门未签到 / 几项作业待交 / 几门博雅可签」，不用切页也不用进专门页面。点击对应项直接跳转（未签到 → iClass，待交 → 作业视图，可签 → BYKC），点击「待交」会自动同时切换页签和视图。
- 指示条只报告**已加载**的数据源：没加载过的来源显示为空而不是 0，避免在从未拉取作业时谎称「今日无待办」；全部已加载且无事可做时才显示 `✓ 今日无待办`。
- 未签到只统计今天、且有签到 ID、且确实未签到的课程；待交只统计未提交且未过期的作业（过期作业不再是待办）；博雅取门户标记的当前可签到/可签退课程。

- 希冀作业详情：按 `o` 打开详情面板，显示每道题的得分/满分与提交状态。此前我们已经在下载详情页，却只取了标题、时间和总分，把题目明细整张表丢掉了——现在解析出来不额外发请求。
- 作业状态不再只看“是否提交过”：3 道题里交了 2 道会显示为未提交，列表行也会显示 `题目 2/3`（未交完为黄色），避免“已提交”掩盖还差一题。
- 作业详情显示 `58 / 100` 形式的得分与满分，得分按得分率着色（≥85% 绿、≥60% 黄、更低红），截止时间按紧迫度着色（已过红、24 小时内黄加粗、更远蓝）。
- SPOC 作业详情按需拉取说明正文（列表接口不返回正文），首次打开时加载并缓存；HTML 会先转成纯文本再显示，块级标签转成换行。
- 作业详情面板独占整屏、支持滚动，与 BYKC 详情一致；CLI `tasks` 表格新增 `problems` 列，`--json` 自动带上题目明细与满分。

- 成绩支持全部学期：TUI 成绩页按 `A` 并发拉取所有学期（上限 4 并发），CLI 用 `grades --all`（与 `--term` 互斥）。
- 成绩新增学分加权统计：GPA、加权平均分、总学分、计入/跳过门数、不及格门数。无学分或无数值绩点的记录（合格/不合格、待出分、免修）不计入，但会单独计数；某学期失败不会影响其他学期，失败的学期会逐个报出。
- 研究生学期代码（如 `20261`）与成绩门户要求的 `2025-2026-1` 形状不同，会在请求前过滤掉，避免每个研究生学期都发一次注定失败的请求。

- 新增课程课表工作区，专注跟随 UBAA 的课程能力，不引入图书馆、预约、打卡等非课程模块。
- 支持本科 BYXT 和研究生 GSMIS 整学期课表导入，按学期和周次浏览课程、地点、教师与节次。
- 课表按账号本地缓存，首次使用手动更新，更新失败不会覆盖旧课表，支持离线查看。
- 课程工作区新增今日课程、课程搜索、考试、成绩、空教室和只读作业摘要。
- 新增课表 Markdown、CSV、JSON、ICS 导出，以及本地课表快照差异比较。
- CLI 新增 `today`、`exams`、`grades`、`classrooms`、`tasks`、`schedule-export` 和 `schedule-diff`。

### 兼容性

- 直连和 WebVPN 登录统一走 SSO 会话；iClass 登录统一通过 MyCenter 跳转解析临时 `loginName`。

### 新增

- 支持鼠标操作：点击顶部页签切换工作区（课表 / iClass / BYKC），点击课程导航切换视图（今日 / 课表 / 考试 / 成绩 / 空教室 / 作业），点击 BYKC 视图切换，点击列表行选中；滚轮滚动当前列表，弹窗打开时滚轮滚动内容、左键关闭。
- 点击目标不是硬编码坐标，而是渲染时按实际布局登记的矩形，因此热区与画出来的东西天然一致；中文标签按终端显示宽度（CJK 双宽）计算，避免热区错位。

### 界面

- 重做工作区外壳：原先每个页签自己堆边框——标签框、重复标签栏的「切换提示」框、会话框、周次框、内容、详情框，再加一个 6 行的「事件」框，其中四个框只放一行内容却各占 3 行。在 40 行终端上等于 12 行边框围着 4 行文字，真正要看的内容只能分到剩下的空间。
- 现在顶栏、状态行、页脚三行都不带边框，只有内容区加一个外框并拿到全部剩余行数：`iClass BUAA · 页签 · 用户 · 版本` / 当前页签的实时状态 / 最新事件 + 快捷键。iClass 页从 7 个框、12 行边框降到 1 个框、4 行边框。
- 事件框改为页脚一行显示最近一条消息，`e` 仍可打开完整日志。
- 六个课程视图（今日/课表/考试/成绩/空教室/作业）改为一行常驻导航，数字键从此可发现；此前每个视图只在各自标题里重复一遍，看不出还有其他视图。
- 今日课程页新增签到进度条；考试、成绩、空教室、作业各自改为一行摘要（考试场次、成绩门数、校区与空闲教室数、待提交作业数）。
- 课表页把「今天」列用强调色边框标出，课程详情压到 3 行无边框区域。
- 登录页改为居中卡片：原先六个带边框的输入框铺满整个终端，外边还套一层全屏框，宽屏下一个只能放四个字符的字段被拉成一百列宽。现在卡片按内容定宽居中，版本和最近事件作为安静的一行上下文放在卡片下方。

- 重做 BYKC 详情浮窗：原先一个 `BYKC 详情` 框里又套一个 `详细信息` 框，既浪费空间又压缩正文，还重复了标题信息。现在只保留一层边框，标题栏直接显示课程名和当前签到状态（可签到 / 可签退 / 不在窗口 / 无需签到）。
- BYKC 详情按「课程 / 签到 / 选课」分组，签到窗口提到前面——这才是打开详情要看的字段；空字段整行省略，不再用 `-` 铺满一列。
- BYKC 详情浮窗支持滚动（`j`/`k`、`↑`/`↓`、`PgUp`/`PgDn`、`g` 回顶）；此前课程简介超出可视区域就被静默截断，无法查看。
- 底部 BYKC 概要面板改为紧凑摘要（约 8 行，适配 10 行可用高度），完整字段仍可经 `o` 浮窗查看。
- BYKC 详情面板改为独占整屏：此前是屏幕中央约四分之三大小的浮窗，四周仍露出工作区自己的边框和文字，与详情内容交错，看起来像详情被遮挡；实际绘制顺序是对的，但视觉上分不清哪些字属于详情。现在详情打开后清空整屏，下面没有任何东西可混淆。
- BYKC 详情按语义高亮：状态（已报绿 / 已满、过期红 / 可报强调色）、人数（未满绿 / 接近满员黄 / 已满红）、签到与签退窗口（进行中绿加粗 / 未到蓝 / 已过灰）、退选截止（仍可退黄 / 已过灰）、地点蓝。中性字段保持正文色，方便扫读。

- 改为 24 位真彩色配色，色调偏低饱和，接近 btop 的观感；进度条使用渐变色。
- 动画只用于表示真实活动：导入课表、加载课程、登录处理中会显示一个小旋转指示，空闲时界面完全静止。
- 今日课程页新增签到进度条；成绩按分段着色，低分与高分一眼可辨。

### 修复

- 修复课表门户判断错误：此前把“BYXT 会话未激活”当成“研究生账号”，导致本科生按 `u` 导入课表时被送到 GSMIS 并报 `研究生课表导入失败: HTTP 401`。现在先探测门户，探测不出时不会冒充研究生课表。
- 修复本科课表导入 `本科课表学期列表失败：HTTP 401`：SSO 登录后 BYXT 并不会自动持有会话，浏览器是靠访问 `https://byxt.buaa.edu.cn/` 触发 SSO 跳转才拿到 BYXT 自己的 cookie。现在探测门户前先走这一步。此前代码用作激活入口的 `/jwapp/sys/homeapp/index.html` 直接请求会返回 404，只能作 Referer。
- 修复主页事件日志区提示 `C 清空` 与 `y 复制最近错误` 无法使用的问题：这两个快捷键此前只在日志弹窗打开后生效。现在任何界面都可使用，输入账号密码或课程搜索时不会误触发。
- 修复 WebVPN 模式下 iClass 登录失败并返回 `ERRCODE=106 / 用户不存在` 的问题。
- 原因：旧流程在 SSO 登录后直接把学号作为 `app/user/login.action` 的 `phone` 参数传给 iClass；真实网页会先进入 WebVPN CAS service，再访问 iClass `jumpMyCenter`，从 302 `Location` 中获取临时 `loginName`，最后用 `loginName` 调 iClass app 登录接口。
- 修复方式：VPN 登录入口改为浏览器实际使用的 `service=https://d.buaa.edu.cn/login?cas_login=true`；HTTP 客户端共享同一个 cookie jar；iClass 登录前用 no-redirect client 逐跳处理 `jumpMyCenter` 的 302 并解析 `loginName`。
- 验证：用真实 WebVPN 链路复现 `ticket -> wengine-vpn-token-login -> token-login -> /`，再验证 `jumpMyCenter -> loginName -> app/user/login.action` 能成功返回 iClass session。
